function nomeDaFuncao(parametro1, parametro2) {
  // instruções
  return resultado;
}
const minhaFuncao = function(parametro1) {
  // instruções
  return resultado;
};
const minhaFuncao = (parametro1) => {
  // instruções
  return resultado;
};

// Para funções com apenas uma expressão e um único parâmetro,
//  pode ser ainda mais concisa:
const dobrar = numero => numero * 2;